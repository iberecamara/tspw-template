import { test } from "@fixtures/fixtures";

test.describe(
  "Template validations - API",
  {
    tag: ["@template", "@api"],
  },
  () => {
    test(
      "Template test",
      { tag: ["@TEMPLATE-0000", "@TC-API-0000"] },
      async ({ templateApiSteps }) => {

      },
    );

  },
);
